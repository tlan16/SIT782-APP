
const initialState = {
  byId: undefined,
  listByColumn: {
    results: [],
    saved:[]
  }
}

const properties = ( state = initialState, action = {} ) => {
    return state
}

export default properties
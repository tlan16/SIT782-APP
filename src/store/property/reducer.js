const initialState = {
  id: undefined,
  price: undefined,
  agency: undefined,
  mainImage: undefined,
  saved: false,
}

const property = (state = initialState, action) => {
   return state
}

export default property;